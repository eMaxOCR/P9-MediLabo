spring.application.name=patient-service
server.port=8081

note-service.url=http://localhost:8083

spring.datasource.url=jdbc:mysql://localhost:3306/medilabo?serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update

# Security
spring.security.user.name=${S2S_USERNAME}
spring.security.user.password=${S2S_PASSWORD}